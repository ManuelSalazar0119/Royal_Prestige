{
  "name": "royal-prestige-app",
  "version": "1.0.0",
  "description": "Gestor de Contactos, Citas y Entrevistas",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
